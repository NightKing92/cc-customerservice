version: '3.8'

services:
  cs-agent:
    build: .
    container_name: bank-cs-agent
    ports:
      - "8080:8080"
    environment:
      - DEEPSEEK_API_KEY=${DEEPSEEK_API_KEY}
    restart: unless-stopped
